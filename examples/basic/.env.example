# Leave both unset to use a local SQLite file (./local.db) for dev.
# Set both to point this same code at a real Turso database.
TURSO_DATABASE_URL=
TURSO_AUTH_TOKEN=

# Leave unset to use fixed, clearly-insecure dev-only defaults (see src/app.ts).
CMS_COOKIE_SECRET=
APP_JWT_SECRET=
PORT=
